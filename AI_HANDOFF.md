# Передача проекта другому ИИ

Дата передачи: 2026-09-01
Репозиторий: `Help-yourself-dvp/Web-site-anaclitic`
Рабочая ветка: `arena/01a03e50-web-site-anaclitic`

> Этот файл предназначен для нового ИИ-разработчика. Перед продолжением работы обязательно прочитайте `PROJECT (web site analitic).md` и `UNIVERSAL AI GUIDE (Единый мастер-промт).md`.

## 1. Пользователь и правила общения

Пользователь не является разработчиком и раньше не занимался подобными проектами.

Нужно:

- писать по-русски и очень простыми словами;
- не использовать технические термины без расшифровки;
- каждый пункт запроса отвечать отдельно;
- действия пользователя описывать пошагово: что открыть, куда нажать, что должно появиться;
- не заставлять пользователя повторять длинную операцию без крайней необходимости;
- перед выдачей новой версии самостоятельно запускать тесты и сборку;
- прямо признавать ошибки и объяснять, что именно исправлено;
- не просить пользователя присылать секреты, cookies, пароли, токены или коды доступа;
- не просить пользователя вручную менять код блоками; если понадобится ручная замена, давать полный файл;
- после законченного этапа сохранять изменения в Git и оставлять короткий итог.

## 2. Главная идея проекта

Нужен бесплатный локальный помощник для личного мониторинга форумов, начиная с темы 4PDA:

<https://4pda.to/forum/index.php?showtopic=1108618>

Пользователь вручную открывает любую страницу темы и нажимает кнопку расширения. Расширение должно:

1. определить сообщения на странице;
2. найти последнюю страницу темы;
3. пройти назад до сохранённой точки отсчёта;
4. добавить только новые сообщения без повторов;
5. сохранить текст, автора, дату, ссылки, цитаты, изображения и связь с сообщениями, на которые отвечают;
6. подготовить материалы для ручной передачи в онлайн-ИИ;
7. принять ответ ИИ;
8. сохранить читаемую выжимку и структурированные Q&A локально.

ИИ не вызывается автоматически. Пользователь сам решает, какой файл передавать онлайн-ИИ.

## 3. Жёсткие ограничения

Нельзя:

- обходить CAPTCHA, 403, 429, логин, robots.txt и ограничения сайта;
- использовать прокси-ротацию, маскировку автоматизации или обход rate limit;
- автоматически отправлять запросы в веб-интерфейс ИИ;
- автоматически читать ответы веб-интерфейса ИИ;
- коммитить cookies, логины, токены, личные данные и диагностические логи с приватными URL.

При CAPTCHA, логине, 403 или 429 сбор должен остановиться с понятным сообщением.

## 4. Что реализовано

### Расширение

Каталог исходников: `extension/src/`

Готовая версия для установки без Node.js: `extension/dist/`

Текущая версия: `0.3.2`.

Основные возможности:

- Manifest V3;
- TypeScript;
- IndexedDB;
- 4PDA adapter;
- generic forum/article adapters;
- manual selection adapter;
- checkpoint и дедупликация;
- автоматический поиск последней страницы;
- автоматическое продолжение неполного прохода после лимита страниц;
- остановка на 403/429/CAPTCHA/login;
- сбор страниц с паузой;
- обработка кодировки Windows-1251;
- сбор ссылок и ссылок на сообщения, на которые отвечает пост;
- сбор цитат;
- сбор URL изображений, в том числе из ссылок `/forum/dl/post/...`;
- чтение текста и ссылок внутри уже загруженных спойлеров;
- опциональное скачивание изображений;
- список нескольких сохранённых тем и кнопка открытия выбранной темы;
- необязательная лёгкая проверка 4PDA при запуске браузера с индикатором `+`, `!` или `?`;
- сворачиваемые разделы popup;
- отображение версии и приблизительного размера локальной базы;
- новый PNG-значок расширения.

### Сбор сообщений 4PDA

По диагностическому логу пользователя установлено:

- настоящие тела сообщений: `div.postcolor[id^="post-"]`;
- количество тел на странице было 21;
- имена пользователей: `.normalname`;
- информация пользователя: `.postdetails`;
- блоки спойлеров: `.post-block.spoil.close`;
- служебная разметка имела `id`, начинающиеся с `post-`, поэтому старый алгоритм ошибочно сохранял меню как сообщения.

Текущий адаптер должен брать `postcolor` как тело сообщения, а метаданные искать в строке сообщения и в `#post-member-<postId>`.

### AI-файлы

В разделе 3 popup есть два режима:

1. **Скачать одним файлом для ИИ**
   - можно поставить одну или несколько галочек: Markdown, JSON, TXT;
   - каждый выбранный файл содержит промпт, требования к ответу и все сообщения;
   - JSON дополнительно содержит структурированные поля.

2. **Скачать с разбивкой на части**
   - используется только для очень больших объёмов;
   - создаёт prompts `fkb-prompt-01-of-N.md` и другие JSON-файлы;
   - создаёт `fkb-combine-prompt.md` для объединения ответов частей;
   - создаёт `fkb-full-posts.txt` для архива.

В prompt ИИ требуется вернуть:

- валидный JSON схемы 1.0;
- затем строку `---MARKDOWN---`;
- затем полноценную читаемую Markdown-сводку без воды;
- важные новости, решения, проблемы, слухи, ссылки, проверки, Q&A и конфликты;
- URL исходных постов для значимых утверждений.

### Импорт ответа ИИ

Файл: `extension/src/core/importer.ts`.

Импортёр:

- извлекает первый JSON-объект из полного ответа;
- отделяет Markdown после `---MARKDOWN---`;
- принимает отсутствующие необязательные поля;
- восстанавливает `null` в полях, где ожидается строка или массив;
- превращает одиночную строку ссылки в массив;
- добавляет пустой `report.conflicts`;
- добавляет отсутствующие статусы как `unconfirmed`;
- переносит Q&A из Markdown, если в JSON Q&A нет;
- сохраняет человекочитаемую Markdown-сводку в поле `parsed_summary`;
- сохраняет полный исходный ответ в `raw_ai_response`;
- сообщает, что JSON был автоматически восстановлен.

Важное исправление для последней ошибки пользователя: тесты должны покрывать не только пропущенные поля, но и значения `null` и одиночные строки.

### Локальная база

Основные IndexedDB stores:

- `sources`;
- `posts`;
- `runs`;
- `reports`;
- `qa`.

Есть действия:

- **Удалить только неправильные записи** — удаляет очевидное меню и испорченную кодировку, оставляет точку отсчёта;
- **Удалить данные этой темы и начать заново** — удаляет посты, отчёты, Q&A и checkpoint выбранной темы;
- **Удалить всю базу расширения** — удаляет все темы, посты, отчёты и Q&A;
- **Скачать всю базу для редактора** — выдаёт JSON и Markdown без AI-промпта.

Перед опасным удалением должно быть подтверждение.

## 5. Необязательный companion

Каталог: `companion/`

Companion — локальная дополнительная программа на Python/FastAPI/SQLite. Она не нужна для обычной работы расширения.

Она нужна для:

- отдельной локальной веб-панели;
- SQLite и FTS5-поиска;
- хранения большого архива вне IndexedDB;
- Markdown/JSON-экспорта;
- импорта ответа ИИ.

По умолчанию адрес companion должен быть пустым, чтобы пользователь не видел ошибку `Failed to fetch` и не думал, что расширение не работает.

## 6. Последняя проблема пользователя

Пользователь сделал полный сбор примерно 20 страниц форума, получил ответ ИИ и импортировал его обратно.

Импорт показал:

```text
Сохранена Markdown-сводка. Распознано Q&A: 0.
JSON найден, но не прошёл строгую проверку.
report.conflicts должен быть массивом строк.
report.confirmed_decisions[0].status должен быть строкой.
report.confirmed_decisions[0].external_urls должен быть массивом строк.
...
```

Это должно быть закрыто текущим tolerant importer. При следующем обращении пользователя нельзя просить его повторно собирать 20 страниц: сначала нужно попросить установить `extension/dist` версии `0.3.2` и повторно импортировать уже имеющийся полный ответ ИИ.

Ожидаемый результат:

- JSON принят после автоматического заполнения пропущенных полей;
- Q&A сохранены, если они есть в JSON или в Markdown;
- полная читаемая сводка видна в разделе **7. Сохранённые выжимки**.

Если пользователь всё ещё получает старые ошибки, первым делом попросить проверить внизу popup именно версию `0.3.2`. Если там старая версия — проблема в том, что браузер загружает старую папку `extension/dist`.

## 7. Что осталось сделать после успешного теста

Приоритет 1 — не ломать базовый сценарий:

1. проверить реальный импорт ответа пользователя в версии 0.3.2;
2. проверить удаление одной темы и всей базы с изменением размера базы;
3. проверить единый AI-файл Markdown/JSON/TXT;
4. проверить повторное продолжение длинного прохода;
5. проверить переключение двух тем.

Приоритет 2 — полезные улучшения:

- восстановление базы из `fkb-local-export.json` с подтверждением;
- сравнение двух последних AI-выжимок;
- автоматическое обновление Q&A и статусов `confirmed/probable/outdated/conflicting`;
- полноценное добавление скачанных изображений в ZIP, а не только URL;
- отдельные уведомления Windows при badge `+`;
- более глубокий поиск исходного сообщения, если родительского поста ещё нет в локальной базе;
- фильтры по теме, автору, дате и тегу;
- история запусков с понятным названием диапазона страниц.

Не добавлять новые крупные функции до проверки основного сценария на реальном браузере пользователя.

## 8. Команды проверки

Из корня проекта:

```bash
npm install
npm run check
npm run format:check
npm run build
python -m compileall -q companion
```

`npm run check` запускает TypeScript typecheck, ESLint, TypeScript tests и Python tests.

На последнем проверенном этапе успешно проходили:

- 17 TypeScript tests;
- 5 Python tests;
- TypeScript typecheck;
- ESLint;
- Prettier;
- сборка расширения.

## 9. Git и доставка

Работать только в ветке:

```text
arena/01a03e50-web-site-anaclitic
```

Не создавать и не использовать другие ветки.

Готовая папка `extension/dist` намеренно хранится в Git, чтобы пользователь без разработки мог загрузить расширение в Chrome/Edge.

Не коммитить:

- `node_modules`;
- `.venv`;
- `companion/data`;
- SQLite-файлы пользователя;
- diagnostic logs;
- cookies, токены и персональные данные.

Последний смысловой релиз до этой передачи: `0.3.2`. При изменении версии обновлять единый `version.json`, package-файлы и заново собирать `extension/dist`.
